plugins {
    id 'java'
    id 'org.springframework.boot' version '4.1.0'
    id 'io.spring.dependency-management' version '1.1.7'
}

group = 'org.ict'
version = '0.0.1-SNAPSHOT'
description = 'DateManagerBackend'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

repositories {
    mavenCentral()
}

dependencies {
    /* spring-data-jpa 라이브러리 */
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    /* spring security 라이브러리 */
    implementation 'org.springframework.boot:spring-boot-starter-security'
    /* 구글/카카오 소셜로그인(OAuth2 Client) 라이브러리 */
    implementation 'org.springframework.boot:spring-boot-starter-security-oauth2-client'
    /* spring-web 라이브러리*/
    implementation 'org.springframework.boot:spring-boot-starter-webmvc'
    /* 웹 소켓 라이브러리*/
    implementation 'org.springframework.boot:spring-boot-starter-websocket'
    /* lombok 라이브러리 */
    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'
    /* 개발 핫 리로드 라이브러리 */
    developmentOnly 'org.springframework.boot:spring-boot-devtools'
    /* 오라클 라이브러리 */

    /* JWT 토큰 라이브러리 */
    implementation 'io.jsonwebtoken:jjwt-api:0.13.0'
    runtimeOnly 'io.jsonwebtoken:jjwt-impl:0.13.0'
    runtimeOnly 'io.jsonwebtoken:jjwt-jackson:0.13.0'

    /* springdoc 라이브러리(REST API 명세서) */
    implementation 'org.springdoc:springdoc-openapi-starter-webmvc-ui:3.0.3'
    /* 환경 변수 관리용 dotenv-java 라이브러리*/
    implementation 'io.github.cdimascio:dotenv-java:3.0.0'

    /* 프로필 사진 업로드용 AWS S3 SDK */
    implementation platform('software.amazon.awssdk:bom:2.29.52')
    implementation 'software.amazon.awssdk:s3'

    runtimeOnly 'com.oracle.database.jdbc:ojdbc11'

    /* 숙박업 CSV(서울 열린데이터광장) 파싱용 - 주소 필드에 콤마가 섞여있어 단순 split으로는 못 나눔.
       좌표계 변환(EPSG:5174 -> WGS84)은 proj4j로 시도했다가 정확도 문제(약 350m 오차)로 그만두고
       data/convert_lodging_csv.py(pyproj)가 미리 처리하도록 바꿔서, 자바 쪽엔 좌표계 라이브러리가
       따로 필요 없다. */
    implementation 'org.apache.commons:commons-csv:1.11.0'
    testImplementation 'org.springframework.boot:spring-boot-starter-data-jpa-test'
    testImplementation 'org.springframework.boot:spring-boot-starter-security-test'
    testImplementation 'org.springframework.boot:spring-boot-starter-security-oauth2-client-test'
    testImplementation 'org.springframework.boot:spring-boot-starter-webmvc-test'
    testImplementation 'org.springframework.boot:spring-boot-starter-websocket-test'
    testCompileOnly 'org.projectlombok:lombok'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
    testAnnotationProcessor 'org.projectlombok:lombok'
}

tasks.named('test') {
    useJUnitPlatform()
}
